# Fake Circuit Board Traces

Generates decorative circuit-board trace artwork using A* pathfinding (`astar.js`) to route traces, exported as SVG (`canvas2svg.js`).

Live at <https://circuits.mechstack.dev/>
